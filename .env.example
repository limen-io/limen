# Gmail API credentials for the slice 1 send_email adapter.
# Obtain a refresh token manually via Google OAuth Playground; this is the
# Daniel-personal-account static-token model from first-slice.md §3.
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_SENDER=
