version: 1

rules:
  - id: deny-email-outside-allowlist
    description: Block any email recipient outside the local test allowlist
    deny_when:
      to:
        not_in:
          - allowed@example.com
